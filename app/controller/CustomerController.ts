import { Request, Response, NextFunction } from 'express';

export const SignUp = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in SignUp" })
    }
}

export const LogIn = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in LogIn" })
    }
}

export const VerifyAccount = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in VerifyAccount" })
    }
}

export const Otp = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in Otp" })
    }
}

export const Profile = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in Profile" })
    }
}

export const UpdateProfile = (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        res.send({ message: "Something wrong in UpdateProfile" })
    }
}
